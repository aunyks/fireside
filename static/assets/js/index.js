function generateItem(title, picture, description, origin, link){
    // Create item
    var item = $('<article></article>');
    // Create link wrapper
    var link = $('<a></a>');
    // Create actual image object
    var img = $('img');
    // Set img source
    
    // Wrap image in link
    link.append(img);
    
    // Create title
    var titleTxt = $('<h3></h3>');
    // Set titleTxt a title
    
    // Create description
    var descTxt = $('<p></p>');
}

$(document).ready(function(){
    
    // Get server info
    
    // Create content items
});